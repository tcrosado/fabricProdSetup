version: '2'

networks:
  byfn:

services:
  kafka1.example.com:
      container_name: kafka1.example.com
      extends:
        file: ../Base/docker-compose-base.yaml
        service: kafka1.example.com
      networks:
        - byfn         
